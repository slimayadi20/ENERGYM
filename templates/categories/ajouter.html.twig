
{% extends "base-back.html.twig" %}
{% block body %}
    {{ include('Dashboard/navbar.html.twig') }}
    {{ include('Dashboard/leftSideBar.html.twig') }}

    <div class="content-page">
        <!-- Start content -->

        <div class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="card-box">
                            <h4 class="m-t-0 header-title">create Categories</h4>
                            <div class="row">
                                <div class="col-12">
                                    <div class="p-20">

                                        {{ form_start(form_Categories, {attr: {novalidate: 'novalidate'}}) }}

                                        <form class="form-horizontal" role="form">

                                            <div class="form-group row">
                                                <label class="col-2 col-form-label">Nom</label>
                                                <div class="col-10">
                                                    {{ form_widget(form_Categories.nom) }}
                                                    {{ form_errors(form_Categories.nom, {'attr':{'class':'text-danger mb-1 '}})}}
                                                </div>
                                            </div>

                                            <a class="btn btn-primary" href="{{ path('categories') }}">Retour</a>
                                            <input type="submit" value="ajouter"   class="btn btn-success">
                                            {{ form_end(form_Categories) }}

                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

