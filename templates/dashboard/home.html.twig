
<div class="content-page">
    <div class="content">
        <div class="container-fluid">
            {% if app.user %}
                <li><a href="">Logout</a></li>
            {% else %}
                <li><a href="">Login</a></li>
            {% endif %}
                    <div class="card-box">
                        <div class="row">
                        <div class="col-xl-3 col-md-6">
                            <div class="card bg-primary text-white mb-4">
                                <div class="card-body d-flex align-items-center justify-content-between">
                                    <p>User</p>

                                    <p>counter</p>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between">
                                    <a class="small text-white stretched-link" href="{{ path('user') }}">View Details</a>
                                    <div class="small text-white"><i class="mdi mdi-subdirectory-arrow-right"></i></div>
                                </div>
                            </div>
                        </div>
                        <div class="col-xl-3 col-md-6">
                            <div class="card bg-primary text-white mb-4">
                                <div class="card-body d-flex align-items-center justify-content-between">
                                    <p>Reclamation</p>

                                    <p>counter</p>
                                </div>
                                <div class="card-footer d-flex align-items-center justify-content-between">
                                    <a class="small text-white stretched-link" href="{{ path('reclamation') }}">View Details</a>
                                    <div class="small text-white"><i class="mdi mdi-subdirectory-arrow-right"></i></div>
                                </div>
                            </div>
                        </div>
                           </div>
                    </div>
                </div>
            </div>
    </div>
